name: rag-project
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - jupyterlab
  - ipykernel
  - numpy
  - pyyaml
  - tqdm
  - pip:
      - langchain>=0.2
      - langchain-community>=0.2
      - langchain-experimental
      - chromadb>=0.5
      - faiss-cpu
      - rank-bm25
      - sentence-transformers>=2.7
      - torch==2.5.1
      - huggingface-hub
      - FlagEmbedding
      - ollama
      - pypdf
      - pymupdf
      - beautifulsoup4
      - black
      - ruff
      - pytest
      - jupytext
